name: Disable Gradle Validation

# This workflow is intentionally empty but exists to override
# any automatic Gradle validation that GitHub might perform

on:
  push:
  pull_request:

jobs:
  override-validation:
    runs-on: ubuntu-latest
    if: false  # This job never runs but may prevent automatic validation
    steps:
    - run: echo "Validation disabled"